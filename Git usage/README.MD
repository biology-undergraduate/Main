This specific repository contains the analysis of git usage through time and can be run using the command:

```

Rscript src/make-plot.r

```

The input data is in 'data' and the results are in 'out'
